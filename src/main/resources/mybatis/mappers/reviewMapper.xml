<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shinhan.travelTogether.review">
	<select id="selectAll" resultType="reviewvo">
		select r.*, m.nickname
		from tt_review r join tt_member m on(r.member_id=m.member_id)
	</select>
	<select id="selectMyreviewAll" parameterType="int" resultType="reviewvo">
		select r.*, m.nickname
		from tt_review r join tt_member m on(r.member_id=m.member_id)
		where member_id=#{member_id}
	</select>
	<select	 id="selectMyreviewByFundingId" resultType="int">
		select r.funding_id
		from tt_review r join tt_member m on(r.member_id=m.member_id)
	</select>
	<insert id="insertMyreview" parameterType="reviewvo">
		insert into tt_review(review_id, review_content, create_date, like_count, views, funding_id, member_id)
		values(review_seq.nextval, #{review_content}, #{create_date}, #{like_count}, #{views}, #{funding_id}, #{member_id})
	</insert>
	
</mapper>