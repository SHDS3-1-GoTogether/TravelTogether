<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shinhan.travelTogether.randomfunding">
	<select id="selectAmountAll" parameterType="int" resultType="String">
		select price
		from tt_funding f
		where f.member_id =(select member_id from tt_member where is_manager = 1 and member_id = #{id})
	</select>
</mapper>